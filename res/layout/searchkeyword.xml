<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="match_parent" >

    <RelativeLayout
        android:id="@+id/search_layout_index_top"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:layout_alignParentTop="true"
        android:background="@drawable/title_bar" >

        <ImageButton
            android:id="@+id/search_btn_back"
            android:layout_width="50dp"
            android:layout_height="fill_parent"
            android:layout_alignParentLeft="true"
            android:layout_centerVertical="true"
            android:layout_margin="5dp"
            android:layout_marginLeft="5dp"
            android:background="@drawable/back_selector" />

        <EditText
            android:id="@+id/search_edit_search"
            android:layout_width="fill_parent"
            android:layout_height="34dp"
            android:layout_centerVertical="true"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:layout_toRightOf="@id/search_btn_back"
            android:background="@drawable/search_bar_button_background_normal"
            android:gravity="center_vertical"
            android:hint="@string/searchhint"
            android:imeOptions="actionSearch"
            android:paddingLeft="20dp"
            android:singleLine="true"
            android:textSize="12sp" />

        <ImageView
            android:id="@+id/search_img_search"
            android:layout_width="wrap_content"
            android:layout_height="28dp"
            android:layout_alignLeft="@id/search_edit_search"
            android:layout_alignTop="@id/search_edit_search"
            android:layout_margin="2dp"
            android:src="@drawable/ic_btn_search" />
    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/search_layout_middle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/search_layout_index_top"
        android:background="#eaebeb" >

        <ListView
            android:id="@+id/search_list_keyword"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:cacheColorHint="#00000000"
            android:divider="@drawable/divide_line" >
        </ListView>
    </RelativeLayout>

</RelativeLayout>